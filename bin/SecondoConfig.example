; --- Secondo Configuration ---
;
; This is an example file. copy or rename it to SecondoConfig.ini

[Environment]
;;; Home directory of Secondo database, in fact the place where the Berkeley DB will
;;; store all record files used by Secondo.
SecondoHome=/secondo/databases

;;; Algebralevel after startup (Executable|Descriptive|Hybrid). 
AlgebraLevel=Executable

;;; Name of the local socket of the Secondo registrar
RegistrarName=SECONDO_REGISTRAR

;;; Name of the executable of the Secondo registrar program
RegistrarProgram=SecondoRegistrar

;;; Name of the executable of the Secondo listener program
ListenerProgram=SecondoListener

;;; Host address of the Secondo server
SecondoHost=127.0.0.1

;;; Port of the Secondo server (default: )
SecondoPort=1234

;;; Runtime Flags (RTFlags). This key can be used to define a list of runtime
;;; flags which switch on some special messages (for debugging or profiling) or
;;; some features like creating binary list formats for CS Communication. 
;
; Server:BinaryTransfer   : This flag controls the generation of binary lists.
; Server:ResultFile       : If set, a result.bnl file is created on the server side.
; Server:SendTimeMsg      : Measure the time for writing the result list to the socket object. 
;
; SI:QueryTime     : Measure cpu and real time used for processing a query.
; SI:ListMemory    : Show Information about used list memory.
; SI:RelStatistics : Various statistical information about tuples and relations.
; SI:ResultList    : Print out nested list of the result before copying it into the applications list memory.
;
; FText:Trace      : Print out messages in methods of the FText algebra. 
;
; Socket:SendStat  : Show how often the requested buffersize could not be transferred and the number of sleep calls; 
;
RTFlags=SI:QueryTime

; --- Berkeley DB Section ---
;
; If Secondo is configured to use the Berkeley DB for persistent
; storage, the parameters in this section need to be specified

[BerkeleyDB]
;;; Name of the executable of the Secondo server program
ServerProgram=SecondoServerBDB

;;; Name of the executable of the Berkeley DB checkpoint program
CheckpointProgram=SecondoCheckpoint
CheckpointTime=5
;;; Directory where logging files are stored (may be relative to home directory)
;LogDir=
;;; Cache Size in kB
CacheSize=4096
;;; Lock System Configuration
;MaxLockers=
MaxLocks=2048
;MaxLockObjects=

; --- Oracle DB Section ---
;
; If Secondo is configured to use an Oracle database for persistent
; storage the parameters in this section need to be specified

[OracleDB]
;;; Name of the executable of the server program
ServerProgram=SecondoServerORA
;;; Oracle connect string for the Secondo database (usually a TNS name)
ConnectString=SECONDO
;;; Oracle user id for the Secondo database instance
SecondoUser=SECONDO_MGR
;;; Password of the Oracle user
SecondoPswd=SECONDO_MGR

; --- Context sections ---
;
; To allow fine tuning of the storage management it is possible to
; define several "context" sections which can be referenced when
; creating or opening SmiFiles. The "Default" context section lists
; all possible parameters.

[Default]
;Bdbxxx=
;OraTableSpace=
;OraIndexSpace=

;;;;;; End of Secondo Configuration ;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;

