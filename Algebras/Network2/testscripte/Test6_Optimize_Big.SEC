#################################################################
# Testszenario for testing the operator optimize_network
#################################################################

open database berlinmod;

## Test 1 ######################
query optimizenet(B_NETWORK);

query print_adjacencylist(B_NETWORK);

close database;
